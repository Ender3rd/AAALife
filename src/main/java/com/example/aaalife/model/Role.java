package com.example.aaalife.model;

public enum Role {
    Customer,
    Adjuster,
    // these are not used in the current implementation, but are predicted when using the full architecture.
    Payments,
    Underwriting,
    CustomerSupport,
    Admin,
    Developer
}